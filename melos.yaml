name: drift_offline
repository: https://github.com/huntergps/drift_offline

packages:
  - packages/*
  - packages/drift_build

command:
  version:
    linkToCommits: true

  bootstrap:
    runPubGetInParallel: false
    usePubspecOverrides: true

scripts:
  analyze:
    run: >
      melos exec
      --ignore="drift_supabase_generators"
      --ignore="drift_offline_first_with_supabase_build"
      --ignore="drift_supabase"
      --ignore="drift_offline_first_with_supabase"
      --ignore="example_odoo_contacts"
      -- dart analyze .
    description: Run dart analyzer on stable packages (Supabase packages excluded pending supabase pub.dev dep validation)

  analyze-rest:
    run: >
      melos exec
      --scope="drift_rest"
      --scope="drift_rest_generators"
      --scope="drift_offline_first_with_rest"
      --scope="drift_offline_first_with_rest_build"
      -- dart analyze .
    description: Analyze REST packages (separate from main analyze to track REST stability)

  analyze-supabase:
    run: >
      melos exec
      --scope="drift_supabase"
      --scope="drift_supabase_generators"
      --scope="drift_offline_first_with_supabase"
      --scope="drift_offline_first_with_supabase_build"
      -- dart analyze .
    description: Analyze Supabase packages (requires supabase pub.dev package)

  test:
    run: melos exec -- dart test
    description: Run tests for all packages

  format:
    run: melos exec -- dart format .
    description: Format all packages

  build:
    run: melos exec --depends-on="build_runner" -- dart run build_runner build --delete-conflicting-outputs
    description: Run build_runner on packages that use it
