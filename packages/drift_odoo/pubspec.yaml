name: drift_odoo
description: Odoo JSON-2 API provider for Drift offline-first packages. Includes HTTP client, offline queue, and OdooProvider.
version: 0.1.0
homepage: https://github.com/huntergps/drift_offline
repository: https://github.com/huntergps/drift_offline
issue_tracker: https://github.com/huntergps/drift_offline/issues
# publish_to: none — path: dependencies must be replaced with version refs before publishing.
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  drift_odoo_core:
    path: ../drift_odoo_core
  collection: ">=1.15.0 <2.0.0"
  http: ">=1.0.0 <2.0.0"
  logging: ">=1.0.0 <2.0.0"
  meta: ">=1.3.0 <2.0.0"
  sqflite_common: ">=2.5.0 <3.0.0"
  synchronized: ">=3.0.0 <4.0.0"

dev_dependencies:
  lints:
  sqflite_common_ffi: ">=2.3.0 <3.0.0"
  test: ">=1.24.0 <2.0.0"
