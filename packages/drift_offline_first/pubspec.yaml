name: drift_offline_first
description: Base offline-first repository for Drift. Provider-agnostic policies, hydration, and subscriptions.
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  collection: ">=1.15.0 <2.0.0"
  drift: ">=2.0.0 <3.0.0"
  http: ">=1.0.0 <2.0.0"
  logging: ">=1.0.0 <2.0.0"
  meta: ">=1.3.0 <2.0.0"
  sqflite_common: ">=2.5.0 <3.0.0"

dev_dependencies:
  drift_dev: ">=2.0.0 <3.0.0"
  lints:
  test:
