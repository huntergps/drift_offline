name: drift_supabase_generators
description: Code generators for drift_supabase — generates fromSupabase/toSupabase adapters from @ConnectOfflineFirstWithSupabase annotations.
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

publish_to: none

dependencies:
  analyzer: ">=6.0.0 <7.0.0"
  drift_build:
    path: ../drift_build
  brick_core: ">=1.0.0 <2.0.0"
  brick_json_generators: ">=3.0.0 <4.0.0"
  build: ">=2.0.0 <3.0.0"
  drift_supabase:
    path: ../drift_supabase
  source_gen: ">=1.0.0 <2.0.0"

dev_dependencies:
  lints:
  test:
