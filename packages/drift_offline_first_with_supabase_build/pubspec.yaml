name: drift_offline_first_with_supabase_build
description: Build runners and code generators for drift_offline_first_with_supabase. Generates Supabase adapters and the model dictionary.
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

publish_to: none

dependencies:
  analyzer: ">=6.0.0 <7.0.0"
  drift_build:
    path: ../drift_build
  build: ">=2.0.0 <3.0.0"
  drift_offline_first_with_supabase:
    path: ../drift_offline_first_with_supabase
  drift_supabase:
    path: ../drift_supabase
  drift_supabase_generators:
    path: ../drift_supabase_generators
  source_gen: ">=1.0.0 <2.0.0"

dev_dependencies:
  lints:
  test:
